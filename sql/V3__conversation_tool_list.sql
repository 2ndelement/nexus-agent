-- ============================================================================
-- conversation 表新增字段
-- ============================================================================
ALTER TABLE `conversation` ADD COLUMN `tool_list` JSON COMMENT '会话开始时的可用工具列表';

-- ============================================================================
-- Index 优化
-- ============================================================================
ALTER TABLE `conversation` ADD INDEX `idx_tool_list_update` (`tenant_id`, `user_id`, `update_time`);
